{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "go run build build",
    "format": "go run build format",
    "go-install": "go run build install",
    "lint": "go run build lint",
    "test": "go run build test"
  },
  "dependencies": {
    "cspell": "^8.17.3",
    "prettier": "^3.5.1",
    "prettier-plugin-packagejson": "^2.5.8"
  }
}
